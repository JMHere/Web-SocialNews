import Popup from "reactjs-popup";

interface postPopupProps {
    setOpen: any,
    open: any
}

function PostPopUp({setOpen, open}: postPopupProps) {
    return (
        <div>
        </div>
    );
}

export default PostPopUp;